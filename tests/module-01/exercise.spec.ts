import { test, expect } from '@playwright/test';

test('Module 01: Basic Navigation', async ({ page }) => {
  // -----------------------------------------------------------------------
  // EXERCISE: Use Copilot to generate the navigation code.
  // -----------------------------------------------------------------------

  // 1. Write a comment below asking to navigate to the DHCW homepage.
  // Tip: Type "// Navigate to the homepage"



  // 2. Use Copilot to verify the title.
  // Tip: Type "// Expect the title to contain 'Digital Health and Care Wales'"


});
