import { test, expect } from '@playwright/test';

test('Module 02: Locators & Actions', async ({ page }) => {
  await page.goto('/');

  // -----------------------------------------------------------------------
  // EXERCISE: Search functionality
  // -----------------------------------------------------------------------

  // Test: Search for "Modern Video Consultation" and click the first result

});
