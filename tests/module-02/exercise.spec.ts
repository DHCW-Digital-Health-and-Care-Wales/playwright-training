import { test, expect } from '@playwright/test';

test('Module 02: Locators & Actions', async ({ page }) => {
  await page.goto('/');

  // -----------------------------------------------------------------------
  // EXERCISE: Search functionality
  // -----------------------------------------------------------------------

  // 1. Ask Copilot to find the search button (magnifying glass) and click it to open the search bar.
  // Note: On the DHCW site, you might need to click a search icon to reveal the input.



  // 2. Type "Modern Video Consultation" into the search input.



  // 3. Press Enter to submit the search.



  // 4. Assert that results are displayed.

});
