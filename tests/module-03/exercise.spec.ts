import { test, expect } from '@playwright/test';
// TODO: Import AxeBuilder from @axe-core/playwright
// import { AxeBuilder } from '@axe-core/playwright';

test('Module 03: Accessibility', async ({ page }) => {
  await page.goto('/');

  // -----------------------------------------------------------------------
  // EXERCISE: Accessibility Scan
  // -----------------------------------------------------------------------

  // 1. Use Copilot Chat to ask how to initialize AxeBuilder and scan the page.
  // Prompt: "How do I scan this page for accessibility violations using AxeBuilder?"



  // 2. Assert that there are no violations.

});
